import React, { memo } from 'react';
import * as Styled from "./styled";

export const Sports = memo(() => {
  return (
    <Styled.Wrapper>
      <Styled.Container>Sports</Styled.Container>
    </Styled.Wrapper>
  );
});
